% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/urls.R
\name{banc_shorturl}
\alias{banc_shorturl}
\title{Shorten a BANC Neuroglancer URL}
\usage{
banc_shorturl(
  x,
  baseurl = NULL,
  cache = TRUE,
  state_server = "https://global.daf-apis.com/nglstate/post",
  ...
)
}
\arguments{
\item{x}{Either a fragment-encoded Neuroglancer URL (character)
or an \code{ngscene} object as returned by
\code{\link[fafbseg:ngl_decode_scene]{fafbseg::ngl_decode_scene()}}.}

\item{baseurl}{Optional override for the Spelunker base URL.
Defaults to the public BANC instance.}

\item{cache}{Logical; cache the POST so repeated calls in the same
session don't re-hit the state server.}

\item{state_server}{URL of the Neuroglancer state-storage endpoint.}

\item{...}{Passed to \code{\link[fafbseg:ngl_encode_url]{fafbseg::ngl_encode_url()}}.}
}
\value{
A character short URL of the form
\verb{https://spelunker.cave-explorer.org/#!middleauth+https://global.daf-apis.com/nglstate/api/v1/<id>}.
}
\description{
Posts a Neuroglancer scene to the public \code{nglstate} server and
returns a short Spelunker URL that round-trips the full state via
a server-side state id. Useful for sharing scenes (e.g. embedding
them in articles or sending them in chat) without the kilobyte-scale
fragment that a fully inline-encoded scene carries.
}
\examples{
\dontrun{
u <- banc_scene()
short <- banc_shorturl(u)
}
}
